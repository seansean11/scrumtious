import React, { Component, PropTypes } from 'react';

const Accessible = ({ customeEl }) => {
  return (
    <customEl>
      {this.props.children}
    </customEl>
  );
};

export default Accessible;
